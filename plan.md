# План разработки MVP «Рабочий планировщик»

**Основание:** `tz.md`, версия 1.1 от 18.08.2026.  
**Целевая папка:** `D:\0 кодекс\Planer work`.  
**Исполняемые файлы:** только `index.html`, `styles.css`, `app.js`.

## Правила выполнения плана

- Реализовывать только функции, указанные в `tz.md`.
- Каждый шаг заканчивать работающим и проверенным результатом.
- Не переходить к следующему шагу, пока автоматические проверки текущего шага не пройдены.
- Для автоматических проверок разрешены временные тестовые файлы; перед сдачей их нужно удалить из итогового набора.
- Пользователю передавать на ручную проверку только внешний вид, понятность и удобство, а также системный голосовой ввод, который нельзя достоверно проверить без установленной программы.
- Публикацию на GitHub Pages выполнять только после отдельного подтверждения пользователя на последнем шаге.

## Шаг 1. Каркас приложения и локальные данные — ✅ выполнен 18.08.2026

**Статус:** реализация завершена, автоматические проверки пройдены.

### Готовый результат

Страница напрямую открывается в Chrome без сервера и ошибок консоли. Созданы три исполняемых файла, базовый интерфейс, единая модель организаций и задач, пустое состояние и безопасное хранение в `localStorage`.

### Что реализовать

- Создать семантический каркас `index.html`, подключить `styles.css` и `app.js` относительными путями.
- Задать утверждённую светлую палитру, системные шрифты и основные состояния интерфейса.
- Реализовать единый массив `tasks`: дата `YYYY-MM-DD` означает календарную задачу, `date = null` — задачу блокнота.
- Реализовать организации со стабильными идентификаторами и нормализацией названий.
- Реализовать чтение и сохранение состояния по ключу `workPlanner.state.v1`.
- Не сохранять фильтры, открытый месяц, окна и незавершённые формы.
- Безопасно выводить пользовательский текст без выполнения HTML.
- Обработать ошибку записи и повреждённый JSON без автоматического удаления данных.

### Автоматическая проверка

- Проверить наличие ровно трёх исполняемых файлов и корректность их подключений.
- Проверить синтаксис JavaScript и отсутствие ошибок консоли при прямом открытии `index.html`.
- Проверить создание уникальных идентификаторов и правила полей задачи и организации.
- Проверить цикл сохранения: записать состояние, перезагрузить страницу и сравнить восстановленные данные.
- Проверить, что повреждённый JSON не перезаписывается, а ошибка записи не показывается как успешное действие.
- Проверить, что HTML в описании и названии организации отображается как обычный текст.

### Ручная проверка пользователя

Проверено пользователем по скриншоту 18.08.2026: `index.html` открывается, базовый экран отображается, видна надпись **«Локальные данные готовы»** — PASS. Окончательная оценка внешнего вида выполняется после шага 2.

### История выполнения

- Созданы только три исполняемых файла: `index.html`, `styles.css`, `app.js`.
- Реализованы базовый экран, утверждённая палитра, единая модель `tasks`, модель организаций и ключ `workPlanner.state.v1`.
- Добавлен внутренний объект `window.WorkPlannerApp` для использования моделью и интерфейсом на следующих шагах; отдельной пользовательской функции он не создаёт.
- Проверка корневого состояния намеренно минимальная: версия и наличие массивов `organizations` и `tasks`. Миграции, резервные копии и расширенная схема не добавлялись согласно границам MVP.
- Повреждённый JSON не перезаписывается; очистка доступна только после отдельного подтверждения. Ошибка записи не отображается как успешное действие.
- Пользовательский текст выводится через `textContent`; внешние URL, сеть и `innerHTML` не используются.
- Автопроверка: синтаксис JavaScript — PASS; состав и относительные подключения файлов — PASS; 11 тестов модели, сохранения, восстановления и ошибок `localStorage` — PASS.
- Временный файл автоматических тестов после успешного прогона удалён.
- По присланному скриншоту подтверждены загрузка базового экрана и готовность локальных данных. Скриншот намеренно не содержит календаря и рабочих кнопок: они относятся к шагу 2 и последующим шагам.
- Выявленный нюанс интерфейса: подсказка **«Выберите день или создайте задачу в блокноте»** появилась раньше самих календаря и блокнота, поэтому на техническом экране шага 1 может восприниматься как отсутствующая функция. На шаге 2 она будет заменена рабочим календарным интерфейсом.
- 18.08.2026 расширение Chrome установлено пользователем; соединение Codex с Google Chrome проверено — PASS.
- Ограничение проверки: политика безопасности браузерной автоматизации запрещает агенту самостоятельно переходить по локальным `file://`-адресам. Это не влияет на ручной запуск приложения двойным щелчком, уже подтверждённый скриншотом. Прямой запуск через `file://` остаётся ручной проверкой; остальная доступная логика проверяется автоматически без обхода этого ограничения.
- Отклонений от функций шага 1 и дополнений к продукту нет.

## Шаг 2. Календарь и основной интерфейс — ✅ выполнен 18.08.2026

**Статус:** реализация завершена, автоматические проверки пройдены; визуальная оценка пользователя ожидается.

### Готовый результат

Работает календарь месяца на русском языке: навигация по месяцам, кнопка «Сегодня», сетка 7×6 с понедельника, ячейки дней, счётчики и всплывающее окно выбранного дня. При `1366×768` и масштабе 100% страница не прокручивается.

### Что реализовать

- Собрать верхнюю панель, поиск и фильтры как видимые элементы, строку дней недели и сетку месяца.
- Показывать месяц и год, короткие даты и русские названия.
- Оставлять ячейки соседних месяцев пустыми и неактивными.
- Показывать в дне общий счётчик задач и до трёх уникальных организаций с адаптивным сокращением.
- Реализовать открытие окна дня поверх календаря, внутреннюю прокрутку, закрытие крестиком и автоматическое закрытие при смене месяца.
- Возвращать клавиатурный фокус в исходную ячейку дня после закрытия окна.
- Обеспечить адаптивность для предусмотренных разрешений и масштаба Chrome.

### Автоматическая проверка

- Проверить построение всех месяцев, февраль високосного года, первый день недели и постоянную сетку 7×6.
- Проверить кнопки предыдущего месяца, следующего месяца и «Сегодня».
- Проверить неактивность пустых соседних ячеек.
- Проверить открытие и закрытие окна дня, закрытие при смене месяца и возврат фокуса.
- Проверить счётчики и список организаций на заранее подготовленных данных.
- Проверить отсутствие прокрутки документа при `1366×768` и 100%, отсутствие перекрытий при 125% и 150%, а также режим `1920×1080`.

### Ручная проверка пользователя

- Устраивают ли пропорции календаря, размеры текста и цветовая гамма.
- Хорошо ли читаются дни, счётчик и названия организаций.
- Удобны ли расположение основных кнопок и окно выбранного дня.
- По скриншотам пользователя от 18.08.2026 отсутствие прокрутки документа подтверждено. После исправления адаптивной высоты требуется повторно посмотреть календарь при увеличенном масштабе.

### История выполнения

- Технический экран шага 1 заменён рабочим месячным календарём на русском языке: месяц и год, дни с понедельника, постоянная сетка 7×6, пустые неактивные ячейки соседних месяцев.
- Реализованы кнопки предыдущего и следующего месяца и «Сегодня».
- В ячейке дня выводятся общий счётчик задач и до трёх уникальных названий организаций. При уменьшении доступного пространства CSS последовательно оставляет два, одно или ни одного названия, сохраняя дату и счётчик.
- Реализовано модальное окно выбранного дня поверх календаря с полным списком задач, внутренней прокруткой и закрытием крестиком. После ручного закрытия клавиатурный фокус возвращается в исходную ячейку; при смене месяца окно закрывается автоматически.
- Поиск, фильтры, «Блокнот», «Создать новую задачу» и «Создать задачу» показаны в предусмотренных местах, но намеренно отключены: их логика относится к шагам 3 и 5.
- Для экранной компоновки заданы фиксированная высота приложения, запрет прокрутки документа, шесть гибких строк календаря и отдельные правила для высоты до 760 и 620 px, а также ширины до 1180 и 900 px.
- Автопроверка: синтаксис JavaScript — PASS; структура HTML и ограничения CSS — PASS; 13 из 13 тестов календаря, навигации, високосного года, счётчиков, окна дня, фокуса и сохранения данных — PASS.
- Во время автопроверки обнаружен и исправлен нюанс: первоначальная перерисовка сетки при открытии окна удаляла исходную кнопку дня и мешала вернуть фокус. Теперь выбранная ячейка подсвечивается без её пересоздания.
- Временный файл автоматических тестов после успешного прогона удалён.
- Ограничение проверки без обхода правил безопасности: автоматическая проверка фактической геометрии локальной страницы в Chrome через `file://` недоступна. Кодом и статическими тестами подтверждены отсутствие прокрутки документа и адаптивные правила; пропорции, перекрытия при 125%/150% и удобство окна остаются в ручной визуальной проверке пользователя.
- По присланным скриншотам уточнена адаптивность: при увеличенном масштабе правило для малой высоты скрывало панель фильтров, главный блок занимал строку по содержимому, а гибкая строка оставалась пустой. Панель сохранена в компактном виде, а шапка, панель и календарь явно закреплены за тремя строками экрана; календарь снова должен заполнять всё доступное место.
- Повторная ручная проверка от 18.08.2026 выявила слишком агрессивное скрытие подписей организаций при увеличенном масштабе: сначала оставались только счётчики, после первого исправления — только одна подпись даже при свободном месте. Ограничение по высоте окна полностью удалено. В данные ячейки всегда передаются до трёх организаций; CSS сокращает их число только на узких экранах, а внутренний `overflow` ячейки ограничивает вывод при фактической нехватке высоты.
- Отклонений от функций шага 2 и дополнений к продукту нет.

## Шаг 3. Создание задач, организации и блокнот — ✅ выполнен 18.08.2026

**Статус:** реализация завершена, автоматические проверки пройдены; визуальная оценка пользователя ожидается.

### Готовый результат

Задачи создаются из выбранного дня и общей кнопкой. Работают одиночные и общие задачи, свободный ввод организаций и блокнот для задач без даты. При назначении даты задача с тем же `id` исчезает из блокнота и появляется в календаре.

### Что реализовать

- Реализовать форму задачи с организациями, датой и описанием.
- Добавлять организацию по `Enter`, показывать подсказки и исключать дубли без учёта регистра.
- Соблюдать ограничения: название организации до 100 символов, описание от 1 до 3000 символов, не более пяти организаций.
- Для календарной задачи требовать 1–5 организаций; в блокноте разрешать 0–5.
- Создавать задачу выбранного дня с заранее заполненной датой и разрешать очистить дату для отправки в блокнот.
- Общей кнопкой открывать пустую форму без даты и организаций.
- Показывать заголовок организации или «Общая задача · N».
- Реализовать блокнот: просмотр, редактирование, копирование, удаление и назначение даты.
- При назначении даты перемещать существующую задачу в календарь; для повторного использования оставлять оригинал через предварительное копирование.
- Реализовать кнопку «В блокнот» для невыполненной календарной задачи.

### Автоматическая проверка

- Проверить оба пути создания и их начальные значения.
- Проверить обязательность полей, длины, нормализацию пробелов, запрет шестой организации и повторное использование существующей организации.
- Проверить одиночный и общий заголовки, одну общую галочку и поиск общей задачи через каждую организацию.
- Проверить границы даты: сегодня и эта же дата следующего года разрешены, прошедшие даты и следующий день после годовой границы запрещены.
- Проверить перемещение между блокнотом и календарём без изменения `id` и без появления второй записи.
- Проверить копирование задачи блокнота с новым `id` и независимость копии.
- После каждого действия перезагрузить страницу и проверить восстановление результата из `localStorage`.

### Ручная проверка пользователя

- Удобно ли вводить и удалять организации.
- Понятно ли различаются одиночная и общая задачи.
- Удобны ли форма создания, блокнот и выбор даты.

### История выполнения

- Активированы общая кнопка «Создать новую задачу», кнопка «Блокнот» и создание из выбранного допустимого дня.
- Реализована единая форма с организациями, датой и стандартным многострочным `textarea`: общая кнопка открывает пустую форму без даты, а создание из дня заранее подставляет выбранную дату. Дату можно очистить, чтобы сохранить задачу в блокноте.
- Организации добавляются по `Enter`, кнопкой или выбором подсказки; отображаются отдельными удаляемыми метками. Пробелы нормализуются, регистр не создаёт дубль, существующий `id` используется повторно, шестая организация блокируется.
- Реализованы проверки описания 1–3000 символов, названия организации 1–100 символов, 0–5 организаций для блокнота и 1–5 для календарной задачи.
- Реализован локальный диапазон планирования от сегодняшней даты до этой же календарной даты следующего года включительно. Для 29 февраля граница корректно сокращается до 28 февраля следующего невисокосного года. Прошедший день можно открыть для просмотра, но кнопка создания в нём отключена.
- Изменение по ручному запросу пользователя от 18.08.2026: горизонт всех операций с датой расширен с 30 дней до одного календарного года. Единая граница применяется к созданию, копированию, редактированию, назначению даты и будущему ручному переносу; это уточнение заменяет прежнее ограничение ТЗ в 30 дней.
- Одиночная задача показывает название организации, общая хранится одной записью и показывает «Общая задача · N» со всеми организациями в компактных метках.
- Реализован блокнот без счётчика: задачи без даты сортируются по `updatedAt` от новых к старым и доступны для редактирования, независимого копирования, удаления с подтверждением и назначения даты.
- При назначении даты изменяется существующая запись с тем же `id`: она исчезает из блокнота и появляется первой среди невыполненных задач выбранного дня. Для повторного использования оригинал сохраняется только через предварительное копирование.
- У невыполненной календарной задачи добавлена кнопка «В блокнот» с подтверждением. Она сохраняет `id`, организации и описание, очищает календарные поля и переносит ту же запись в блокнот.
- На момент завершения шага 3 редактирование задачи блокнота не назначало дату: для этого использовалась отдельная кнопка «Назначить дату». Позднее это правило изменено ручным уточнением пользователя на шаге 4; актуальное поведение зафиксировано в истории шага 4.
- Исправление по ручной проверке пользователя от 18.08.2026: в форме копирования задачи блокнота поле даты разблокировано. Копию можно оставить без даты либо сразу создать календарной в допустимом диапазоне; исходная задача всегда остаётся в блокноте. Это уточнение пользователя заменяет прежнее правило о строго бездатной копии блокнота.
- По той же ручной проверке базовые кнопки «Редактировать», «Копировать» и «Удалить» добавлены в календарную карточку раньше шага 4. Редактирование сохраняет изменения, копия получает новый `id`, удаление требует подтверждения. Галочка, ручной/автоматический перенос, стрелки и режим несохранённых изменений по-прежнему остаются шагу 4.
- Новые задачи и копии добавляются в начало внутреннего массива. Это обеспечивает детерминированное первое место в блокноте даже при совпадении `updatedAt` до одной миллисекунды; календарный порядок по-прежнему задаётся полем `order`.
- После удаления задачи неиспользуемые организации удаляются из внутреннего справочника и перестают попадать в подсказки.
- Уведомление показывается только после успешной записи единого состояния в `localStorage`; при ошибке текущее состояние приложения не подменяется успешным.
- Автопроверка: синтаксис JavaScript — PASS; структура HTML и внутренние прокрутки — PASS; 17 из 17 тестов создания, формы, ограничений, организаций, общих задач, дат, блокнота, копирования, перемещений, удаления, безопасности и восстановления после перезагрузки — PASS.
- Целевая автопроверка после ручного замечания: 6 из 6 сценариев — PASS. Проверены доступная дата копии из блокнота, сохранение оригинала, датированная копия, наличие календарных действий и фактические нажатия редактирования, копирования и удаления с записью в `localStorage`.
- Целевая автопроверка годового горизонта: 6 из 6 сценариев — PASS. Проверены обе включённые границы, запрет следующего дня, сохранение задачи на последнюю дату, атрибуты `min`/`max` формы, активная кнопка создания в последнем допустимом дне и переход 29.02.2024 → 28.02.2025.
- Временный файл автоматических тестов после успешного прогона удалён.
- Интерактивная галочка, ручной и автоматический перенос, порядок `↑`/`↓`, полная раскрываемая карточка и обработка несохранённых изменений намеренно не добавлялись: они относятся к шагу 4.
- Отклонений от функций шага 3 и дополнений к продукту нет.

## Шаг 4. Полный жизненный цикл задачи — ✅ выполнен 18.08.2026

**Статус:** реализация завершена, автоматические проверки пройдены; визуальная оценка пользователя ожидается.

**Уточнение после шага 3:** базовые кнопки календарного редактирования, копирования и удаления уже реализованы по ручному замечанию пользователя. На шаге 4 их нужно встроить в полную раскрываемую карточку, дополнить правилами выполненной задачи и проверить совместно с остальным жизненным циклом, не создавая вторую реализацию.

### Готовый результат

Работают просмотр полной карточки, редактирование, галочка выполнения, ручной и автоматический перенос, копирование, удаление и изменение порядка. Все действия сохраняются и правильно восстанавливаются после перезагрузки.

### Что реализовать

- Раскрывать только одну полную карточку с описанием и доступными действиями.
- Реализовать редактирование с визуальным режимом, отметкой `★` и диалогом несохранённых изменений.
- Реализовать галочку: выполненные задачи приглушаются и уходят вниз; снятие галочки возвращает задачу вверх или переносит прошлую задачу на сегодня.
- Запретить прямой перенос выполненной задачи, оставив копирование.
- Реализовать ручной перенос только в диапазоне от сегодня до этой же даты следующего года включительно.
- Реализовать автоперенос просроченных невыполненных задач на сегодня при загрузке, возврате видимости и смене даты.
- Выделять автоперенесённые задачи утверждённым цветом и значком без хранения старой даты.
- Реализовать порядок `↑` и `↓` внутри группы выполнения и отключать его при активном поиске или фильтре.
- Реализовать подтверждение удаления без корзины и восстановления.

### Автоматическая проверка

- Последовательно нажать и проверить все кнопки полной карточки с учётом доступности действия.
- Проверить установку и снятие галочки, положение выполненных и невыполненных задач и сохранение порядка.
- Проверить все три варианта диалога несохранённых изменений.
- Проверить ручной перенос на граничные даты и запрет недопустимых дат.
- Смоделировать пропуск нескольких дней и убедиться, что просроченные задачи переносятся один раз непосредственно на сегодня в правильном порядке.
- Проверить очистку отметки автопереноса после редактирования, выполнения и ручного переноса.
- Проверить независимость копии, новый `id`, пустую галочку и сохранение оригинала.
- Проверить подтверждения удаления одиночной, общей и находящейся в блокноте задачи.
- Перезагрузить страницу после каждого типа изменения и сравнить данные и порядок.

### Ручная проверка пользователя

- Хорошо ли различаются обычная, выполненная, автоперенесённая и редактируемая задачи.
- Удобно ли расположены действия полной карточки, стрелки порядка и выбор даты переноса.
- Понятны ли подтверждения, уведомления и отметка `★`.

### История выполнения

- В окне дня задачи сначала показаны компактно. По нажатию раскрывается одна полная карточка; раскрытие другой автоматически сворачивает предыдущую. В полной карточке видны дата, все организации, полное описание, галочка и доступные действия.
- Реализована активная галочка выполнения. Выполненная задача приглушается и уходит после невыполненных; при снятии галочки возвращается в начало невыполненной группы. Если галочка снята у задачи прошедшего дня, задача переносится на сегодня и получает признак автопереноса.
- Прямые кнопки «Перенести» и «В блокнот» у выполненной задачи скрыты. Редактирование не разрешает менять её дату, но независимое копирование остаётся доступным и создаёт невыполненную задачу.
- Режим редактирования визуально выделен. После сохранённого изменения задача получает отметку `★ изменено`; у задачи блокнота звезда добавляется к заголовку. При попытке закрыть изменённую форму показан диалог с тремя отдельными исходами: «Отмена», «Не сохранять» и «Сохранить».
- Ручной перенос открывается только из полной карточки невыполненной задачи. Доступен единый согласованный диапазон от сегодня до этой же календарной даты следующего года включительно; перенос в тот же день и даты за пределами диапазона отклоняются.
- Автоперенос выполняется при загрузке, возврате вкладки в видимое состояние и фокусе окна. Все просроченные невыполненные задачи переносятся непосредственно на сегодня один раз, старые места и история переноса не сохраняются; порядок между ними определяется прежней датой и внутренним порядком.
- Автоперенесённая задача выделена отдельным цветом и значком `↷ перенесено`. Признак снимается после редактирования, выполнения или ручного переноса.
- Стрелки `↑` и `↓` меняют порядок только внутри своей группы выполнения. Граничная стрелка блокируется. Отключение стрелок при активном поиске или фильтре будет подключено одновременно с самими поиском и фильтрами на шаге 5; до этого шага эти элементы интерфейса намеренно неактивны.
- Копия получает новый `id`, пустую галочку и независимые данные. При копировании невыполненной календарной задачи на тот же день она располагается непосредственно после оригинала; во время автопроверки был найден и точечно исправлен прежний порядок, помещавший копию в начало группы.
- Кнопка «В блокнот» из полной карточки требует подтверждения, сохраняет ту же запись и очищает дату. Удаление одиночной, общей и находящейся в блокноте задачи подтверждается отдельно; корзина и восстановление не добавлялись.
- После каждого проверенного изменения состояние сравнивалось с фактической записью в `localStorage`. Дополнительно проверено восстановление после нового запуска и автоматический перенос непосредственно при чтении сохранённых данных.
- Автопроверка: синтаксис JavaScript — PASS; структура HTML и визуальные CSS-состояния — PASS; 14 из 14 сценариев жизненного цикла, кнопок, порядка, галочки, ручного и автоматического переноса, несохранённых изменений, копирования, всех видов удаления и восстановления — PASS.
- Временный файл автоматических тестов после успешного прогона удалён.
- Ограничение проверки без обхода правил безопасности: встроенный браузер обнаруживает открытую локальную страницу, но запрещает программное управление адресом `file://`. Внешний вид, различимость состояний и удобство действий остаются в ручной визуальной проверке пользователя.
- Повторная ручная проверка пользователя от 18.08.2026 выявила два связанных дефекта свёрнутого представления. В ячейке календаря общая задача показывала названия входящих организаций вместо подписи «Общая задача», а декоративный знак статуса в строке не менял состояние: нажатие раскрывала карточку со второй настоящей галочкой и повторным заголовком. Вывод дня переведён на подписи задач: для нескольких организаций отображается «Общая задача». В строке оставлена одна настоящая галочка с отдельной зоной нажатия; она меняет статус без раскрытия, а раскрытие выполняется соседней кнопкой. В раскрытых деталях удалены вторая галочка и повторный заголовок. Целевая автопроверка: 3 из 3 сценариев — PASS, включая запись статуса в `localStorage`.
- Промежуточное ручное уточнение пользователя от 18.08.2026 исключало выполненную задачу из краткого списка календарной ячейки: счётчик и подписи временно учитывали только невыполненные задачи. Целевая автопроверка этого промежуточного варианта прошла 2 из 2 сценариев. Позднее пользователь уточнил желаемый вид: выполненная задача должна оставаться непосредственно в ячейке бледной и зачёркнутой. Актуальное итоговое поведение зафиксировано ниже.
- Ручная проверка блокнота от 18.08.2026 выявила, что в режиме «Изменить задачу блокнота» дата оставалась заблокированной и могла назначаться только отдельной кнопкой списка. Поле даты и кнопка «Без даты» разблокированы в самой карточке редактирования. Сохранение с датой перемещает ту же запись с тем же `id` из блокнота в календарь; сохранение с пустой датой изменяет задачу и оставляет её в блокноте. Отдельная кнопка «Назначить дату» сохранена как быстрый путь.
- По дополнительному уточнению пользователя выполненная задача остаётся в полном списке дня не только приглушённой, но и зачёркнутой. Зачёркиваются заголовок и описание, а галочка и действия сохраняют читаемость. Целевая автопроверка этого изменения совместно с назначением даты из блокнота: 3 из 3 сценариев — PASS, включая запись назначенной даты и сохранение в `localStorage`.
- Следующее уточнение отображения выполненных задач от 18.08.2026 заменило промежуточное скрытие из календарной ячейки. Счётчик снова включил все задачи дня, а выполненная подпись получила бледный цвет, зачёркивание и приглушённую точку. Первоначальный вариант объединял одинаковые подписи и ограничивался тремя строками; его целевая автопроверка прошла 3 из 3 сценариев. Последующая проверка заполненного дня изменила объединение и лимит; актуальное поведение зафиксировано ниже.
- Ручная проверка заполненного дня от 18.08.2026 выявила, что одинаковые подписи нескольких общих задач объединялись: счётчик показывал три задачи, а в ячейке оставалась только одна строка «Общая задача». Объединение удалено — каждая задача формирует собственную строку, поэтому две общие задачи видны отдельно. На широком десктопном экране превью расширено до четырёх строк; пятая и последующие задачи продолжают учитываться счётчиком и доступны в полном окне дня. На узких экранах ранее согласованная адаптивность по-прежнему сокращает число строк. Целевая автопроверка: 3 из 3 сценариев — PASS; проверены четыре отдельные строки, две одинаковые подписи общих задач, зачёркивание выполненной строки, ограничение пятой строки и полная запись в `localStorage`.
- После целевой и общей статической проверки проект подготовлен к первому локальному Git-коммиту как контрольная точка завершённого шага 4. Удалённые временные тесты в коммит не включаются; публикация в удалённый репозиторий не выполняется.
- Дополнение пользователя после контрольного коммита от 18.08.2026: добавлен альтернативный постоянный просмотр задач выбранного дня на левой стороне экрана. Верхняя кнопка «Отобразить задачи» открывает левую панель и меняется на «Скрыть задачи». При активной панели нажатие на календарный день обновляет её содержимое без открытия всплывающего окна; после скрытия панели прежнее окно дня снова используется.
- Левая панель по умолчанию показывает сегодняшний день, если открыт текущий месяц. При переходе на другой месяц выбор очищается и появляется подсказка выбрать день; кнопка «Сегодня» возвращает текущую дату и сразу обновляет панель. Выбранная календарная ячейка сохраняет визуальное выделение.
- В постоянной панели доступны тот же полный список, раскрытие одной карточки, галочка, редактирование, перенос, блокнот, копирование, удаление, стрелки порядка и создание задачи выбранного дня. Используется та же логика и те же записи `localStorage`, отдельная копия функционала не создавалась.
- Компоновка основного экрана переключается между полноширинным календарём и двумя колонками: постоянная панель слева, календарь справа. Высота остаётся ограниченной экраном, а прокрутка списка выполняется только внутри панели. Для узких экранов панель сужается, кнопка её включения остаётся доступной.
- Автопроверка постоянной панели: 8 из 8 сценариев — PASS. Проверены структура, верхняя кнопка, выбор дня без модального окна, раскрытие и галочка, сохранение в `localStorage`, создание с выбранной датой, возврат к окну дня, навигация месяца, «Сегодня» и отсутствие прокрутки документа.
- По ручному уточнению кнопка постоянной панели перенесена из правой группы действий в левую часть шапки под названием «Рабочий планировщик». Для визуального отделения от зелёных кнопок создания и блокнота она получила золотисто-бежевый цвет из палитры проекта; во включённом состоянии фон становится насыщенно-золотым. Текст и `aria-pressed` по-прежнему переключаются между режимами «Отобразить задачи» и «Скрыть задачи».
- После визуальной оценки пользователь выбрал варианты UI/UX `1.1`, `2.2` и `3.2`; они заменили промежуточное золотистое оформление. Раскрытая карточка теперь выделяется светло-зелёным фоном, зелёной рамкой и мягкой тенью, а у свёрнутых обычных карточек левая полоса стала нейтральной. Цветовые признаки выполненной и автоматически перенесённой задачи сохранены; раскрытие по-прежнему не меняет данные.
- Текстовые кнопки действий раскрытой задачи заменены компактными иконками: редактирование, перенос, перенос в блокнот, копирование и удаление. Каждая иконка имеет русское доступное имя, системную и визуальную подсказку при наведении или фокусе; удаление получает красный акцент только при взаимодействии. Существующие обработчики, запрет переноса выполненной задачи и стрелки порядка не изменены.
- Переключатель постоянной панели остался слева под названием, но получил нейтральный бело-серый контур и пиктограмму панели; включённое состояние обозначается мягким зелёным фоном. Текст меняется в отдельном элементе, поэтому пиктограмма не исчезает при переключении.
- Автопроверка выбранных UI/UX-правок: 18 из 18 статических сценариев — PASS; проверены классы состояний, подписи и подсказки, пять действий и их прежние обработчики, ограничения выполненной задачи, переключатель и отдельный опасный стиль удаления. `node --check app.js` и `git diff --check` — PASS. Программная визуальная проверка локальной страницы не выполнена: политика браузерного управления блокирует `file://`; для оценки различимости, понятности иконок и цвета остаётся ручное обновление страницы пользователем.
- Ручная проверка пользователя выявила двойную подсказку у иконок: одновременно показывались собственная тёмная подсказка и системный `title`. Системный `title` удалён только у иконок задач; единая визуальная подсказка и русское доступное имя `aria-label` сохранены. Действия «Редактировать», «Копировать» и «Удалить» в блокноте переведены на те же иконки, а основное действие «Назначить дату» оставлено текстовой зелёной кнопкой.
- Пользователь согласовал безопасное удаление организации как архивирование. Добавлено окно «Организации»: название можно изменить с сохранением стабильного `id`, поэтому оно сразу обновляется во всех связанных задачах. Команда удаления после подтверждения ставит организации признак `archived`: она исчезает из подсказок и не может использоваться в новой задаче или копии, но остаётся с прежними связями в календарных, выполненных и блокнотных задачах. Старые данные без поля `archived` считаются активными. Повторное название другой активной или архивной организации блокируется.
- Над календарём добавлена навигация колесом мыши: прокрутка вниз открывает следующий месяц, вверх — предыдущий. Один жест ограничен одним переключением с паузой 350 мс, горизонтальная прокрутка игнорируется, а `Ctrl/Command + колесо` не перехватывается и сохраняет масштабирование браузера. При смене месяца используются прежние правила закрытия окна дня и обновления постоянной панели.
- Автопроверка этого дополнения: 23 из 23 итоговых статических сценариев и 5 из 5 функциональных сценариев — PASS. Фактически проверены переименование без смены `id`, сохранение двух связанных задач, запись архива в `localStorage`, запрет архивной организации для новых задач и копий, сохранение её в исторической задаче, направление колеса, ограничение повторного события и неперехват масштабирования. `node --check app.js` и `git diff --check` — PASS; временный тестовый файл удалён.
- Контрольный коммит `8e03b90` остаётся точкой состояния до этого дополнения. Постоянная панель дня и выбранные UI/UX-правки сохранены следующим локальным коммитом с сообщением `feat: add day panel and refine task controls`; публикация в удалённый репозиторий не выполнялась.
- Единая подсказка иконок, иконки блокнота, управление организациями и навигация колесом сохраняются следующим локальным коммитом с сообщением `feat: manage organizations and improve planner controls`; публикация в удалённый репозиторий не выполняется.
- Следующее решение пользователя заменило отдельное управление организациями из предыдущего дополнения. Кнопка и окно «Организации», глобальное переименование и команда архивирования удалены из интерфейса и исполняемого кода. Уже сохранённые организации с признаком `archived` продолжают корректно читаться, оставаться в исторических задачах и не предлагаться для новых — совместимость пользовательских данных сохранена.
- Первая реализация изменения организации из карточки оказалась неверной по смыслу: двойной щелчок открывал форму всей задачи и позволял менять только состав организаций у одной задачи. Пользователь отклонил этот сценарий, поскольку требовалось переименование самой организации. Проверки первой реализации подтверждали ошибочно понятый контракт и не считаются проверкой требуемого поведения.
- Для будущей авторизации зафиксировано архитектурное ограничение: стабильный `organization.id` остаётся связью задач и будущих учётных записей, отображаемое название организации не становится логином или идентификатором. Будущая сущность пользователя должна содержать отдельные `login`, `organizationId` и роль; пароль нельзя хранить в `localStorage`, для него потребуется серверная авторизация. В текущий локальный MVP логины и пароли не добавлялись.
- Исправленный сценарий разделяет два действия. Двойной щелчок по конкретной метке открывает компактное окно **«Изменить организацию»**, а не карточку задачи; `Enter` предоставляет тот же доступ с клавиатуры. Окно прямо сообщает, что новое название появится во всех связанных задачах. Сохранение изменяет только `name`, `normalizedName` и время обновления выбранной организации, не меняет её стабильный `id`, связи задач и будущую связь с учётной записью. Пустое и дублирующее название блокируются. Состав организаций конкретной задачи по-прежнему изменяется только через отдельную команду редактирования задачи.
- Автопроверка исправленного сценария: 11 из 11 функциональных и 12 из 12 статических сценариев — PASS. Проверены бездействие одиночного щелчка, открытие отдельного окна организации двойным щелчком и через `Enter`, отсутствие открытия карточки задачи, загрузка выбранного названия, глобальное переименование, сохранение стабильного `id` и связей календарной и блокнотной задач, обновление заголовка связанной задачи, закрытие после сохранения, запрет пустого и дублирующего названия, уникальность HTML-идентификаторов и наличие стилей фокуса. `node --check app.js` и `git diff --check` — PASS; временный тест удалён. Программная визуальная проверка локальной страницы недоступна из-за блокировки `file://`; пользователю остаётся проверить размер окна, понятность предупреждения и удобство двойного щелчка после `Ctrl+F5`.
- Коммит `772ec14` содержит первую, отклонённую реализацию. Исправление сохраняется следующим локальным коммитом с сообщением `fix: rename organizations from task cards`; публикация в удалённый репозиторий не выполняется.
- Все дополнения к продукту сверх первоначального плана шага 4 перечислены в этой истории выполнения.

## Шаг 5. Поиск, фильтры и итоговая локальная проверка — ✅ выполнен 18.08.2026

**Статус:** реализация завершена, автоматические проверки пройдены; визуальная оценка, проверка HiDu и фактический замер в Chrome ожидаются от пользователя на финальной приёмке.

### Готовый результат

Локальный MVP полностью соответствует функциональной части ТЗ: работают поиск, фильтры, сброс, ошибки, доступность, системный текстовый ввод и все заявленные сценарии. Приложение проверено на нормативном объёме данных.

### Что реализовать

- Реализовать мгновенный поиск только по описанию.
- Реализовать фильтр одной организации и фильтр выполнения.
- Объединять поиск и фильтры условием «И», одной кнопкой полностью сбрасывать их.
- Применять поиск и фильтр организации в блокноте без фильтра выполнения.
- Реализовать пустые состояния, краткие уведомления и все предусмотренные диалоги ошибок.
- Завершить клавиатурное управление, доступные имена, удержание фокуса в диалогах и `prefers-reduced-motion`.
- Обеспечить работу стандартного `textarea` с вставкой, композиционным и системным голосовым вводом.
- Исключить сетевую отправку пользовательских данных, аналитику, секреты и вывод описаний в консоль.

### Автоматическая проверка

- Проверить поиск после каждого символа, регистр, пробелы и отсутствие поиска по названию организации.
- Проверить каждый фильтр, все сочетания через «И» и полный сброс.
- Проверить изменение счётчиков и организаций в ячейках при фильтрации.
- Проверить блокировку стрелок порядка при поиске и фильтрах.
- Прогнать все 30 сценариев использования, пограничные случаи и критерии приёмки из `tz.md`.
- Проверить клавиатурный обход, фокус диалогов, доступные имена кнопок и чекбоксов.
- Имитировать обычный ввод, вставку и композиционное событие в `textarea`.
- Проверить объём: 200 задач месяца, до 10 задач дня, 200 задач блокнота и пять организаций в задаче.
- Измерить переключение месяца в Chrome; целевой результат — не более 200 мс на контрольном компьютере.
- Повторно проверить консоль, `localStorage`, отсутствие сетевых запросов с пользовательскими данными и безопасный вывод текста.

### Ручная проверка пользователя

- Удобно ли искать и фильтровать задачи в реальной работе.
- Не выглядит ли интерфейс перегруженным при большом числе задач.
- Корректно ли HiDu или `Win + H` вводит текст в поле описания на вашем компьютере.

### История выполнения

- Включён мгновенный поиск по описанию. Запрос обрезается по краям, сравнивается без учёта регистра и не проверяет названия организаций.
- Добавлены фильтр одной организации и фильтр выполнения с вариантами **«Все задачи»**, **«☐ Невыполненные»**, **«☑ Выполненные»**. Все активные условия календаря объединяются через логическое **И**. В блокноте применяются только поиск и организация, статус намеренно игнорируется.
- Фильтры влияют на календарные счётчики, подписи в ячейках, всплывающее окно дня, постоянную левую панель и блокнот. Кнопка **«Сбросить»** очищает все условия одним действием; фильтры не записываются в `localStorage` и после перезагрузки остаются пустыми.
- Дополнение пользователя от 18.08.2026: при активном поиске или фильтре постоянная левая панель показывает не только выбранный день, а все подходящие календарные задачи открытого месяца. Результаты отсортированы по дате, у каждой задачи показана короткая дата, а выбор другого дня не сужает список. При переходе между месяцами список обновляется для нового месяца. Создание задачи из панели в этом режиме отключено, поскольку единая дата не выбрана; после полного сброса панель возвращается к обычному режиму выбранного дня. Целевая автопроверка: 12 из 12 сценариев — PASS.
- При пустом результате показываются **«Задачи не найдены»** и локальная кнопка **«Сбросить»**. При активном условии обе стрелки порядка выключены и показана подсказка **«Сбросьте поиск и фильтры, чтобы изменить порядок»**.
- Для клавиатуры добавлено перемещение фокуса по календарной сетке стрелками. Все поля, включая `textarea`, получили заметный зелёный фокус; нативные `dialog`, доступные имена и `prefers-reduced-motion` сохранены.
- Стандартный `textarea` не перехватывает аудио и принимает обычный, вставленный и композиционный текст через одно событие `input`; это обеспечивает совместимость с системной диктовкой без интеграции с HiDu.
- Автопроверка шага: 47 из 47 функциональных сценариев — PASS. Проверены поиск по каждому символу, регистр и пробелы, отсутствие поиска по организации, все фильтры и их сочетания, общие задачи, полный сброс, блокнот, счётчики, пустые состояния, блокировка порядка, клавиатурная сетка, текстовый ввод, основные операции жизненного цикла и запись подтверждённых изменений.
- Статическая проверка: 26 из 26 сценариев — PASS. Подтверждены уникальные HTML-id, доступные подписи, нативные диалоги, зелёный фокус, отсутствие `innerHTML`, исполняемого пользовательского текста, сети, аналитики, секретов и вывода описаний в консоль. Исполняемая часть по-прежнему состоит ровно из `index.html`, `styles.css`, `app.js`; временный тест удалён.
- Нормативные объёмы 200 задач месяца, 10 задач дня, 200 задач блокнота и пять организаций в задаче обработаны без ошибок. Синтетический DOM-тест переключения месяца с 200 задачами занял 2,72 мс. Это не считается фактическим замером Chrome: программное управление локальной страницей `file://` заблокировано политикой браузера. Точный Chrome-замер, внешний вид при большом объёме и HiDu остаются ручными пунктами финальной приёмки.
- Изменения шага 5 подготовлены в рабочей папке и пока не сохранены отдельным Git-коммитом.

## Шаг 6. Финальная приёмка и публикация после подтверждения

### Готовый результат

Получена окончательно проверенная локальная версия. После одобрения пользователем эта же версия опубликована как статический сайт GitHub Pages; функциональность локального и опубликованного вариантов совпадает.

### Что выполнить

- Повторить полный автоматический прогон на чистом `localStorage` и на сохранённых данных.
- Проверить четыре контрольных режима экрана: `1366×768` при 100%, 125% и 150%, `1920×1080` при 100%.
- Удалить временные тестовые артефакты; оставить три исполняемых файла и документацию.
- Передать локальную версию пользователю для итоговой визуальной оценки.
- Публиковать на GitHub Pages только после отдельного подтверждения пользователя.
- После публикации повторить автоматическую проверку основных сценариев и убедиться, что локальная и опубликованная версии используют независимые `localStorage`.

### Автоматическая проверка

- Проверить все критерии приёмки `tz.md` и сохранить фактические результаты, включая измерение производительности.
- Проверить отсутствие ошибок консоли, неработающих кнопок, потери данных после перезагрузки и лишних исполняемых файлов.
- После публикации проверить открытие сайта, относительные пути, все основные действия и отсутствие зависимости от сервера или базы данных.

### Ручная проверка пользователя

- Итогово оценить внешний вид и удобство рабочего процесса на основном компьютере.
- Подтвердить готовность версии к публикации.

До отдельного подтверждения пользователя шестой шаг заканчивается готовой локальной версией и публикация не выполняется.
